//problem statement: insert element at any location in array
#include<stdio.h>
#include <iostream>
using namespace std;
/*
int main()
{
    int a[20], i, n, loc, num;
    cout << "how many elements do you want to enter: ";
    cin >> n;

    cout << "\nenter array elements: ";
    for (i = 0; i < n; i++)
    {
        cin >> a[i];
    }
    cout << "\nenter location where you want to delete elemrnt: ";
    cin >> loc;


    for (i = loc - 1; i <= n - 1; i++)
    {
        a[i] = a[i + 1];
    }
    n--;

    cout << "\n your array elements: ";
    for (i = 0; i < n; i++)
    {
        cout << "\t" << a[i];
    }
    return 0;
}
*/

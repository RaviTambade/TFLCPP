/*
Title -  Dictionay Implementation Using ADT (Abstract Data Type)
	
		I.Implement all the functions of a dictionary (ADT) using hashing 
		II.Handle collisions using chaining with / without replacement.
		III.Data: Set of (key, value) pairs,
			Keys are mapped to values, 
			Keys must be comparable, 
			Keys must be unique. 
			
		Standard Operations: Insert(key, value), Find(key), Delete(key)
*/
#include <stdio.h>
# include./.VSCODE/point.h"
int main(){
    struct point startpoint;
    struct point endpoint;

         startpoint.x=34;
         starpoint .y=43;
         endpoint.x=45
         endpoint.y=24
         printf ("plz fill the detail information");
         printf("enter the value of the x=%d\n",startpoint.x);
         printf("enter the value of the  y=%d\n",startpoint.y);
         printf("enter the value of the x=%d \n",endpoint.x);
         printf("enter the value of the y=%d\n",endpoint.y);
         //shortcut way of the above longterm program 
         printf("x=%d%d,y=%d%d\n",startpoint.x,startpoint.y,endpoint.x endpoint.y);
        

    
}
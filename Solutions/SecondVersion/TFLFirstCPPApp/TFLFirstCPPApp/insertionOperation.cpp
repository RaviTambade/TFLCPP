//problem statement: insert element at any location in array
#include<stdio.h>
#include <iostream>
using namespace std;
/*
int main()
{
    int a[20], i, n, loc, num;
 
    cout << "how many elements do you want to enter: ";
    cin >> n;

    cout << "\nenter array elements: ";
    for (i = 0; i < n; i++)
    {
        cin >> a[i];
    }
    cout << "\nenter location where you want to insert elemrnt: ";
    cin >> loc;

    cout << "\nenter data to be insert: ";
    cin >> num;

    for (i = n - 1; i >= loc - 1; i--)
    {
        a[i + 1] = a[i];
    }
    a[loc - 1] = num;
    n++;

    cout << "\n your array elements: ";
    for (i = 0; i < n; i++)
    {
        cout << "\t" << a[i];
    }
    return 0;
}
*/
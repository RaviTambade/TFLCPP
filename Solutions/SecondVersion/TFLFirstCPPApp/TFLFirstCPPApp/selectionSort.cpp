#include <stdio.h>
#include <stdlib.h>
#include<iostream>
using namespace std;
/*
int main()
{
    int a[10], i, j, num, n, mid, temp, k;
    cout << "enter size of array: ";
    cin >> n;

    cout << "enter elements in array: ";
    for (i = 0; i < n; i++)
    {
        cin >> a[i];
    }

    //selection sort logic
    for (i = 0; i < n - 1; i++)
    {
        k = i;
        for (j = i + 1; j < n; j++)
        {
            if (a[j] < a[k])
            {
                k = j;
            }
        }
        if (i != k)
        {
            temp = a[i];
            a[i] = a[k];
            a[k] = temp;
        }
    }

    cout << "your array elements are: ";
    for (i = 0; i < n; i++)
    {
        cout << "\t" << a[i];
    }
    return 0;
}
*/